/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        //for allowing external images in the app
        domains: ["images.pexels.com"]

    }
}

module.exports = nextConfig
